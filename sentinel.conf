# specify path to neobytes.conf or leave blank
# default is the same as NeoBytesCore
#neobytes_conf=/home/neobytes/.neobytescore/neobytes.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
